# Training / SHAP scripts (src/)
numpy>=1.24
pandas>=2.0
torch>=2.0
mne>=1.6
edfio>=0.4          # MNE's EDF-writer backend, needed by src/augment_eegmat.py's raw.export(...)
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
shap>=0.44

# Downstream analysis / report generation (analysis/)
python-docx>=1.1
